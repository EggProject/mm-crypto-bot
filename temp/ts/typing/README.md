# ts-typing

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ts-typing` to build the library.

## Running unit tests

Run `nx test ts-typing` to execute the unit tests via [Vitest](https://vitest.dev/).
