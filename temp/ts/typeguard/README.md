# ts-typeguard

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ts-typeguard` to build the library.

## Running unit tests

Run `nx test ts-typeguard` to execute the unit tests via [Vitest](https://vitest.dev/).
