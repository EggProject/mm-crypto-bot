# ts-rxjs

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ts-rxjs` to build the library.

## Running unit tests

Run `nx test ts-rxjs` to execute the unit tests via [Vitest](https://vitest.dev/).
