{
  "name": "@mm-crypto-bot/backtest",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'backtest: ts-only, no emit'",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "bun test src",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@mm-crypto-bot/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}