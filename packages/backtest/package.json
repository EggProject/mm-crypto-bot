{
  "name": "@mm/backtest",
  "version": "0.0.0",
  "private": true,
  "description": "Backtest motor váz — historikus adatokon futtatja a kiválasztott stratégiát",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "backtest": "bun run src/index.ts",
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@mm/core": "workspace:*",
    "@mm/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
