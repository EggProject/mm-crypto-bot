{
  "name": "@mm-crypto-bot/backtest",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'backtest: ts-only, no emit'",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "bun test src",
    "coverage": "echo 'Running @mm-crypto-bot/backtest V8 coverage' && bunx vitest run --config vitest.config.ts --coverage",
    "clean": "rm -rf .turbo *.tsbuildinfo coverage",
    "coverage:unit": "echo 'Running @mm-crypto-bot/backtest V8 coverage' && bunx vitest run --config vitest.config.ts --coverage",
    "coverage:text": "echo 'Running @mm-crypto-bot/backtest V8 coverage' && bunx vitest run --config vitest.config.ts --coverage",
    "coverage:lcov": "echo 'Running @mm-crypto-bot/backtest V8 coverage' && bunx vitest run --config vitest.config.ts --coverage"
  },
  "dependencies": {
    "@mm-crypto-bot/core": "workspace:*",
    "@mm-crypto-bot/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
