{
  "name": "@mm-crypto-bot/exchange",
  "version": "0.0.1",
  "description": "CCXT Pro bybit.eu SPOT feed wrapper \u2014 REST + WS illeszt\u00e9s a fels\u0151bb r\u00e9tegekhez",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'exchange: ts-only, no emit'",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "bun test src tests",
    "coverage": "bun test src tests --coverage --coverage-reporter=lcov --coverage-dir=coverage",
    "clean": "rm -rf .turbo *.tsbuildinfo coverage",
    "coverage:text": "bun test src tests --coverage --coverage-reporter=text",
    "coverage:lcov": "bun test src tests --coverage --coverage-reporter=lcov --coverage-dir=coverage",
    "coverage:bybit-eu-adapter": "vitest run --config vitest.bybit-eu-adapter.config.mjs --coverage"
  },
  "dependencies": {
    "@mm-crypto-bot/numeric": "workspace:*",
    "@mm-crypto-bot/shared": "workspace:*",
    "ccxt": "4.5.75"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
