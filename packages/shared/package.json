{
  "name": "@mm-crypto-bot/shared",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts",
    "./config": "./src/config.ts",
    "./utils": "./src/utils.ts",
    "./logger": "./src/logger.ts"
  },
  "scripts": {
    "build": "echo 'shared: ts-only, no emit'",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run --config vitest.config.ts",
    "coverage": "vitest run --config vitest.config.ts --coverage",
    "clean": "rm -rf .turbo *.tsbuildinfo coverage",
    "coverage:unit": "vitest run --config vitest.config.ts --coverage",
    "coverage:text": "vitest run --config vitest.config.ts --coverage",
    "coverage:lcov": "vitest run --config vitest.config.ts --coverage"
  },
  "dependencies": {
    "@mm-crypto-bot/numeric": "workspace:*",
    "ccxt": "4.5.64",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
