# mm-crypto-bot environment example
#
# Active operation is paper/emulated only. This repository template configures
# no private access values, and live activation is unavailable until a
# separately authorized implementation provides it.
#
# Configuration is stored in the validated paper TOML profiles under
# `run-bot/config/`.
