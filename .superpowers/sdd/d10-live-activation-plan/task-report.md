# D-10 live activation evidence ledger

This is an evidence record for the current candidate, not a live-trading authorization or completion claim. No stage or commit was performed.

## Candidate inventory

The current candidate has exactly 20 source/test/config/documentation paths before this report and 21 including this report:

`.env.example`; `README.md`; `apps/bot/README.md`; `apps/bot/src/cli/commands/start-log-routing.test.ts`; `apps/bot/src/cli/commands/start.ts`; `apps/bot/src/config/config-exchange.test.ts`; `apps/bot/src/config/config.test.ts`; `apps/bot/src/config/loader-environment.test.ts`; `apps/bot/src/config/loader.ts`; `apps/bot/test/e2e/runtime-driver/cli-boundaries.ts`; `apps/bot/test/e2e/runtime-driver/config-store.ts`; `docs/LIVE-TRADING.md`; `docs/production-strategies/latency-budget.md`; `docs/production-strategies/pre-launch-checklist.md` (deleted); `run-bot/config/default.toml`; `run-bot/config/live-eu.example.toml` (deleted); `run-bot/config/live-eu.toml` (deleted); `run-bot/config/paper-backtest-optimized.toml`; `run-bot/config/paper-backtest-verified.toml`; `scripts/coverage-tools/bot-runtime-scope.json`.

Every candidate file is at most 500 lines; the report is below 500 lines. Exact line counts are captured in `scans.log`.

## Dispatch ledger

Effective model means profile-enforced; provider-dispatch attestation was not observable. All write tasks used isolated workspace-write and no external/mutable resource.

| Work item / owner                                                  | Class; mode; review role; package count; Terra predicate                                                                                                                                                                    | Dependency/parallel relation; sandbox/authority; retry/fallback; result                                                                                                          |
| ------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Clean design — `/root/d10_live_activation_clean_design`            | discovery/design; read-only; non-review; 1 apps/bot plus root; D-10 lifecycle/config risk and multi-surface reasoning                                                                                                       | none; isolated read-only; no retry/fallback; requested/effective `gpt-5.6-terra/high`, profile-enforced same, provider attestation not observable; PASS                          |
| Loader/config tests — `/root/d10_loader_config_tests`              | implementation/tests; write; non-review; 1; loader/config boundary Terra trigger; owns loader.ts, config.test.ts, config-exchange.test.ts, loader-environment.test.ts, bot-runtime-scope.json                               | design; parallel with start; isolated workspace-write; no fallback; requested/effective `gpt-5.6-terra/high`, profile-enforced same, attestation not observable; PASS            |
| Start block — `/root/d10_live_activation_block`                    | implementation/tests; write; non-review; 1; live activation fail-closed Terra trigger; owns start.ts, start-log-routing.test.ts                                                                                             | design; parallel with loader/config; isolated workspace-write; no fallback; requested/effective `gpt-5.6-terra/high`, profile-enforced same, attestation not observable; PASS    |
| Paper-only docs — `/root/d10_paper_only_docs`                      | documentation/config; write; non-review; 1 plus root; live operator safety Terra trigger; owns .env.example, README.md, apps/bot/README.md, LIVE-TRADING.md, latency-budget.md, three paper TOMLs and deleted live-eu TOMLs | design; parallel; workspace-write; no fallback; requested/effective `gpt-5.6-terra/high`, profile-enforced same, attestation not observable; PASS                                |
| Worktree transfer — `/root`                                        | integration/evidence; write; non-review; 1 plus root; no Terra trigger for evidence                                                                                                                                         | after components; workspace-write; no fallback; PASS, dirty candidate preserved                                                                                                  |
| CLI split migration — `/root/d10_live_activation_block`            | test migration; write; non-review; 1; test architecture Terra trigger; owns cli-boundaries.ts                                                                                                                               | after E2E split; isolated workspace-write; no fallback; requested/effective `gpt-5.6-terra/high`, profile-enforced same, attestation not observable; PASS                        |
| Coverage design — `/root/d10_loader_e2e_coverage_design`           | coverage/evidence design; read-only; non-review; 1 plus root; coverage-integrity Terra trigger                                                                                                                              | after transfer; isolated read-only; no retry/fallback; requested/effective `gpt-5.6-terra/high`, profile-enforced same, attestation not observable; PASS                         |
| Paper-surface audit — `/root/d10_paper_surfaces_audit`             | audit; read-only; non-review; 1 plus root; product/runtime surface Terra trigger                                                                                                                                            | after design; isolated read-only; no fallback; requested/effective `gpt-5.6-terra/high`, profile-enforced same, attestation not observable; PASS                                 |
| Paper remediation — `/root/d10_paper_surface_remediation`          | remediation; write; non-review; 1 plus root; product/runtime surface Terra trigger; owns three paper TOMLs and checklist                                                                                                    | after audit; isolated workspace-write; no fallback; requested/effective `gpt-5.6-terra/high`, profile-enforced same, attestation not observable; PASS                            |
| Loader dead-guard microfix — `/root/d10_paper_surface_remediation` | narrow implementation; write; non-review; 1; loader correctness Terra trigger; owns loader.ts and config.test.ts                                                                                                            | after audit; isolated workspace-write; no fallback; requested/effective `gpt-5.6-terra/high`, profile-enforced same, attestation not observable; PASS                            |
| Config-store E2E — `/root/config_store_split_impl`                 | test coverage; write; non-review; 1; config integrity Terra trigger; owns config-store.ts                                                                                                                                   | after loader microfix; isolated workspace-write; no fallback; requested/effective `gpt-5.6-terra/high`, profile-enforced same, attestation not observable; PASS                  |
| Evidence — `/root/bot_e2e_split_docs_fix`                          | process evidence; write; non-review; 1 plus root; no Terra trigger for evidence                                                                                                                                             | after stable candidate; workspace-write plus unique temp logs; no fallback; requested/effective `gpt-5.6-luna/low`, profile-enforced same, attestation not observable; COMPLETED |

## Failure and rework ledger

PROCESS FAIL addendum: the prior independent style run is retained as historical failure (ESLint exit 1 with 13 errors and 1 warning; unsupported-file Prettier invocation exit 2); same-owner `start.ts` lint-only remediation then passed with max-warnings 0. Loader lint returned exit 1 only for the known existing warning; `start-log-routing.test.ts` is excluded by project-service configuration. A prior independent `coverage:full` run exited 1: only untouched core OWN was 99.8%; backtest-tools was 75.1% with real-CSV tests, so there is no repo-wide PASS.

Candidate identity: base/candidate HEAD `af6932b47f786d2a993054edd880e6d87b2cae9a`; current status has exactly 21 paths including this report. The real index is unchanged and empty. A writable temporary repository snapshot staged exactly 21 paths before this evidence correction; cached diff-check passed. Artifacts: `candidate-pre-addendum.patch` SHA-256 `ffc5af5415abeda659a873bf78dbe5693573453d3bca001efa80e7fc731e5189`; `candidate-name-status.txt` SHA-256 `0c9437ad4d40e9fac88ab251f0148d18f463ff266017a1baf14707202d6ede15`; `candidate-path-list.txt` SHA-256 `4fbfa460b4eeb3bf0d6c027a32b677c282c3a5dd31a066606ecd90530426ad09`. No write-tree or repository-readable tree is claimed. The snapshot is pre-report-correction; the eventual candidate may differ only in task-report evidence/status text until fresh reviews.

Post-review dispatches: the prior independent TECH PASS owner `/root/d10_final_tech_review_v2` is historical, pre-lint-refactor, and superseded; a fresh TECH review remains pending. The later TECH re-review owner `/root/bot_e2e_split_tech_rereview`, `terra_reviewer`, read-only technical review, dependency pre-remediation candidate, requested/effective `gpt-5.6-terra/high` (profile-enforced; provider attestation not observable), read-only authority, no fallback, result FAIL P0 credential reads. Credential-read remediation owner `/root/d10_paper_surface_remediation`, `terra_worker`, write/non-review, exact ownership `loader.ts` + `start-log-routing.test.ts`, dependency TECH P0, requested/effective `gpt-5.6-terra/high` (profile-enforced; provider attestation not observable), isolated workspace-write, no fallback; loader credential code/docs removed, real TOML/default loader plus descriptor-restored throwing/counted getters verified, zero reads/createBot/run/start/log side effects, PASS.

- Original E2E used stale monolith assertions; transferred full E2E later passed 45/45, while loader-focused coverage was 80% statements / 69.56% branches / 81.39% lines at that stage.
- TECH re-review failed P0 because credential reads remained reachable; credential-read remediation removed those reads and verified throwing/counting getters with zero runtime side effects, then passed.
- Fresh TECH review failed P1 on evidence integrity; this final3 addendum remediates the non-addressable tree claim, stale metrics, and missing current candidate artifacts. Fresh TECH and PROCESS re-reviews remain pending.
- A `Bun.TOML.parse` monkeypatch guard approach was impossible; the design was reclassified to the supported boundary.
- The first raw PID attempt returned `EEXIST`; the runner invocation passed twice afterward.
- A unit-coverage full-command timing issue was observed where evidenced in the prior workstream; no fabricated timing is added here.
- TOML files are unsupported by Prettier; this is recorded as an applicability limitation, not a failure.
- `config.test.ts` was excluded by ESLint project-service configuration; the exact supported scoped command is used below.
- Existing loader security warning remains an existing warning; it is not claimed as newly fixed.

## Current validation evidence

Logs are in unique `/tmp/mm-d10-live-activation-evidence-20260826-final3/`; no secrets or user-home paths are recorded. Transient isolated-worktree absolute paths appear in logs and are not credentials or sensitive payloads. SHA-256: `unit.log` `de5db8cbe14d700bac7cb3dd944a06af90852a089bdd8d98aaf455ba679847a1`; `e2e.log` `aff4e555c514c7b2baeced8ca07b0839b086fef7d4203ece456622fa1f88d5e8`; `tooling.log` `cead966f2496d5dbb2fb0802381c9f3d841684b6dacb2cea4dc6584c4194a953`; `config.log` `4a6409afee251eabf735a1370cbee64174b892e0306c2d42de61846d4de1f585`; `style.log` `faf7332d9f2825c99ed1c80efc40c8d4424962e093903cd7e87c998d0fa71221`; `scans.log` `e958dd57e02ddd690ce383c9d99427e2953f5ab50590063b12430848fcfa9fcd`; `candidate-pre-addendum.patch` `ffc5af5415abeda659a873bf78dbe5693573453d3bca001efa80e7fc731e5189`; `candidate-name-status.txt` `0c9437ad4d40e9fac88ab251f0148d18f463ff266017a1baf14707202d6ede15`; `candidate-path-list.txt` `4fbfa460b4eeb3bf0d6c027a32b677c282c3a5dd31a066606ecd90530426ad09`.

- `coverage:bot:unit`: exit 0; statements 1644/1644, branches 883/883, functions 289/289, lines 1558/1558.
- `coverage:bot:e2e`: exit 0; statements 1640/1640, branches 883/883, functions 290/290, lines 1554/1554, cases 45/45, gate PASS.
- `typecheck:coverage-tools` and `coverage:scope`: exit 0; scope reports 21 owned runtime files.
- Bot typecheck/build: exit 0/0.
- Three exact config validates exited 0 and reported paper mode, bybiteu, and leverage 10/1/10. Supported current start lint, loader warning result, Prettier, diff-check, transport/live scans and exact file-size inventory are in final3 logs; TOML/.env are unsupported by Prettier.

## Boundary status

Physical schema removal, runtime removal, exchange-sandbox removal, and manual-endpoint API removal are pending prerequisite-backed future slices. The built-in emulated paper mode remains. This candidate does not claim D-10 complete, live-ready, or permission to place live orders.

## Final review status

Fresh independent TECH review: PASS. Owner `/root/d10_final_tech_review_v2`, role `terra_reviewer`, requested/effective `gpt-5.6-terra/high` (profile-enforced; provider attestation not observable), read-only, exact 21-path candidate base `af6932b47f786d2a993054edd880e6d87b2cae9a`, 0 valid findings. The prior P1 is closed: all nine final3 artifact files exist and their hashes match; no tree claim remains; exact path/name lists and current boundary are recorded; reverse-patch checks cover all non-report paths; the report-only evidence/status delta was explicitly permitted. Current metrics, config results, root `coverage:full` non-PASS limitation, lint warning, transient isolated-worktree paths, and pending physical schema/runtime/exchange-sandbox/manual-endpoint removal are truthful. Live exit 3, zero side effects, `BUN_ENV` rejection, and unchanged paper behavior are recorded. All candidate files are <=500 lines and the real index is empty. Implementation, test, config, and docs files were unchanged after the reviewed snapshot.

Fresh independent PROCESS review: PASS. Owner `/root/d10_final_process_review_v2`, role `luna_process_reviewer`, requested/effective `gpt-5.6-luna/medium` (profile-enforced; provider attestation not observable), read-only, exact 21-path candidate/base `af6932b47f786d2a993054edd880e6d87b2cae9a`, 0 findings. Six closures are confirmed: current start lint pass with loader warning and exclusions stated truthfully; 3/3 config validations; durable final3 TECH PASS; explicit `coverage:full` exit-1 debt; exact base/status/path plus patch artifacts, report-only delta, and real index empty; and all final3 references existing with matching hashes. This is an evidence/status-only addendum; source, test, config, and docs files are unchanged.

Final status: TECH PASS / PROCESS PASS. Commit is authorized only from the exact 21-path candidate after coordinator cached diff-check. Physical removals remain pending; no live/full-goal claim.
